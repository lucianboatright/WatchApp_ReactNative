/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package watch_native_app.app;

import watch_native_app.list.LinkedList;

import static watch_native_app.utilities.StringUtils.join;
import static watch_native_app.utilities.StringUtils.split;
import static watch_native_app.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
